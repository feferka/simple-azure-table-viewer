# VS Code Copilot Instructions

- Verify that the extension project is created in the workspace root.
- Scaffold a VS Code extension that uses an Activity Bar icon and a view to interact with Azure Storage Tables.
- The extension should call Azure CLI commands to list storage accounts, tables, and entity data.
- Display the downloaded JSON in a new editor tab.
- Keep the project minimal and ready to build.
